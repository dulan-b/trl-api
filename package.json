{
  "name": "trl-api",
  "version": "1.0.0",
  "private": true,
  "description": "The Ready Lab - Video Platform API",
  "scripts": {
    "dev": "pnpm --filter @trl/api dev",
    "build": "pnpm --filter @trl/shared build && pnpm --filter @trl/api build",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "dependencies": {
    "translate": "^3.0.1"
  }
}
